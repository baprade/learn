<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect all incoming requests to the public directory for seamless drag-and-drop cPanel deployment
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
