<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # If the request is not already in the public directory, rewrite to public/
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
